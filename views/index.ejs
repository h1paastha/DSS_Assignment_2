<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>UEA - Blogging Site</title>
	</head>
	<body>
		<style>
			/* Style the navigation bar */
			.navbar {
				width: 100%;
				background-color: #555;
				overflow: auto;
				padding-left: 2px;
				padding-right: 3px;
				padding: 8px;
				/* Shadow Effect */
				box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);
			  	}
			/* Navbar links */
			.navbar a {
				float: left;
				text-align: center;
				padding: 10px;
				margin: 2px;
				color: white;
				text-decoration: none;
				font-size: 18px;
				}
			/* Navbar links on mouse-hover */
			.navbar a:hover {
				background-color: #000;
			 	}
			/* Active navbar links */
			.active {
				background-color: #04AA6D;
				}
			/* Add responsiveness - it will automatically display the navbar vertically instead of horizontally on screens less than 500 pixels */
			@media screen and (max-width: 600px) {
				.navbar a {
					float: none;
					display: block;
					}
				}
		</style>
		<!-- Load an icon library -->
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
		<div class="navbar">
			<a class="active" href="#"><i class="fa fa-fw fa-home"></i> Home</a>
			<a href="/users/login"><i class="fa fa-fw fa-user"></i>Login</a>
		</div>
		<% if (messages.logout_msg) { %>
		<div>
			<%= messages.logout_msg %>
		</div>
		<% } %>
	</body>
</html>
